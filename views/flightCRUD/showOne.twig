{% extends 'layout.twig' %}

{% block body %}
<div class="row  flex-nowrap">
    <div class="col-auto col-md-3 col-xl-2 px-sm-2 px-0">
   {% embed 'admin/dashboard.twig' %}
    {% endembed %}
    </div>
    <div class="col py-3">
<div class="container">
<table class="table table-striped table-striped">
    
 {% if (data) %}
    
        
           <tr><td>Nom</td> <td>{{ data.nom}}</td></tr>
        <tr><td>Capacite</td> <td>{{ data.capacite}}</td></tr>
             <tr><td>Date Depart</td><td>{{ data.dateDepart}}</td></tr>
             <tr><td>Date Arrive</td><td>{{ data.dateArrive}}</td></tr>
             <tr><td>Status</td><td>{{ data.stat}}</td></tr>
        
        <tr><td></td>
            <td>
            <a class="btn btn-outline-primary" href="/flights/edit/{{data.id}}"> Edit</a>
           <a class="btn btn-outline-success" href="/flights/delete/{{data.id}}"> 
           Delete
           </a>
           
        </td>
        </tr>

        </table>
{% endif %}
</div>
</div>
</div>
{% endblock %}